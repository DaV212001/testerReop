// forgot_password_controller.dart

import 'package:get/get.dart';

class ForgotPasswordController extends GetxController {
  final double fadeAnimationValue = 0.0; // Initialize with 0.0 for full transparency
  final double slideAnimationValue = -100.0; // Initialize with -100.0 for off-screen position

// Add your password reset logic here (e.g., API calls, email sending, etc.)
}
